This is a UCSB course data refresher class.

CLASS:
RefreshData

METHODS:

public void curriculumSearch(PrintWriter,String,String,String)
This accesses a department and writes all the courses to the department course file

public void RateMyProf()
This updates the ratings list so the proper ratings can be appended to the department course file

public void enumerateDepartments()
This creates 2 departments files. One for reference, the other to be used in a full curriculum search
